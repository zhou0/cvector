interface:
  display_name: "Cvector"
  short_description: "Repo-specific patterns and workflows for cvector"
  default_prompt: "Use the cvector repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true